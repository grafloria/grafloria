registry=https://registry.npmjs.org/

# The Angular 19 toolchain pins @angular/ssr one patch behind build-angular's
# peerOptional range; the lockfile resolution is correct — accept it.
legacy-peer-deps=true
