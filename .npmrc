# DevExpress NuGet Feed Configuration
# This file configures npm to use the DevExpress NuGet feed for DevExpress packages

# DevExpress NuGet Feed URL (with authentication token)
@devexpress:registry=https://nuget.devexpress.com/REMOVED/api/v3/index.json

# Use npm registry for all other packages
registry=https://registry.npmjs.org/
